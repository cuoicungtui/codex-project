---
scope: Skill runtime evaluation loop v1
purpose: Build substrate for the hackathon implementation and future story execution
altitude: feature
updated: 2026-06-27T06:02
---

- (assumption) Assumed fast path and build-substrate output only because the user asked for the next architecture step without requesting a broader audience document.
- (decision) Use a filesystem-first ports-and-adapters pipeline so workspace artifacts stay the source of truth and provider/runtime details stay behind ports.
- (decision) A run workspace is immutable after completion except for append-only trace writes during execution; downstream stages read prior artifacts and write new artifacts rather than mutating existing ones.
- (decision) Model selection is resolved once per run from environment variables through a ModelClient port, and the effective config is persisted into run metadata.
- (decision) Skill versions are explicit filesystem snapshots referenced by run metadata; runs never execute an unversioned latest folder.
- (decision) The implementation flow is fixed as ingest, execute, evaluate, feedback, and compare, with each stage owning its output schema and reading only prior stage artifacts.
- (decision) All durable artifacts use versioned JSON or JSONL schemas with ISO-8601 UTC timestamps so small agents can parse them deterministically.
- (decision) The runtime must enforce workspace-bounded file I/O so skill execution cannot write outside configured roots by default.
- (direction) Do not over-architect provider routing for v1; keep a single provider adapter implementation behind a stable port and defer multi-provider logic.
- (question) Which provider should back the initial ModelClient adapter in v1?
- (question) Should full prompt bodies be persisted by default or only when trace verbosity is elevated?
- (version) Python 3.12.10 adopted from the local execution environment as the pinned runtime seed.
- (event) Mechanical lint passed with zero findings for ARCHITECTURE-SPINE.md.
- (event) spine finalized
- (decision) Agent Skills SDK was selected as the runtime skill framework, with agentskills-core and agentskills-fs as the v1 integration surface.
- (question) The local .env file is currently empty, so provider and model configuration remain unbound until non-secret runtime fields are supplied.
